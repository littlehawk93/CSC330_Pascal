CONST A = 550;

VAR B, C, D, E;

  PROCEDURE ONE;
    VAR M, L;

    FUNCTION TWO;
      CONST A = 44;
      VAR K, L;

      BEGIN
        TWO := K + M * (A - CALL TWO + 5);
        WHILE B = L DO CALL ONE;
      END;

    VAR Q;
    BEGIN
      ;;;
      WHILE B * Q = L DO ;;;
      IF M * A = B + CALL TWO THEN
        BEGIN
        END;
      CALL ONE;
    END;

VAR Z, Y;
BEGIN
  BEGIN
    BEGIN
    END
  END
END.
